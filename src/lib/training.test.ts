import {describe,it,expect} from 'vitest';import {applyChallenge,EMPTY_VECTOR} from './training';import {challenges} from '../data/challenges';describe('adaptive training',()=>{it('moves only targeted dimensions by formula',()=>{const c=challenges[0];const after=applyChallenge(EMPTY_VECTOR,c,100,0);expect(after.clarity).toBeGreaterThan(0);expect(after.depth).toBe(0)});it('applies momentum after 3 correct',()=>{const c=challenges[0];expect(applyChallenge(EMPTY_VECTOR,c,100,3).clarity).toBeGreaterThan(applyChallenge(EMPTY_VECTOR,c,100,0).clarity)})});