import { Link } from 'react-router-dom'; import { challenges } from '../data/challenges';
export default function GymPage(){const ordered=[...challenges].sort((a,b)=>a.difficulty.localeCompare(b.difficulty));return <section className="screen"><p className="eyebrow">Gimnasio</p><h1>25 desafíos cognitivos</h1><p>La recomendación prioriza dimensiones con mayor peso sistémico; el entrenamiento persiste el delta calculado.</p><div className="grid">{ordered.map(c=><article className="card" key={c.id}><strong>{c.title}</strong><p>{c.prompt}</p><p>{c.difficulty} · XP {c.xpReward}</p><Link className="button" to={`/training/${c.id}`}>Entrenar</Link></article>)}</div></section>}